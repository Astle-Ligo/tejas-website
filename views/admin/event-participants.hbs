<h2 class="mb-4">Event Registrations for {{eventDetails.eventName}}</h2>
<h3>{{this.eventDetails.eventDate}}</h3>

{{#if registrations.length}}
<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th scope="col">#</th>
            <th scope="col">Event Name</th>
            <th scope="col">Participant Type</th>
            <th scope="col">Participants</th>
            <th scope="col">Class</th>
            <th scope="col">Phone Number</th>
            <th scope="col">Registration Time</th>
        </tr>
    </thead>
    <tbody>
        {{#each registrations}}
        <tr>
            <th scope="row">{{inc @index}}</th>
            <td>{{this.eventDetails.eventName}}</td>
            <td class="text-capitalize">{{this.type}}</td>
            <td>
                {{#if (eq this.type "individual")}}
                {{this.participant.name}} ({{this.participant.regNum}})
                {{else}}
                <strong>Team Name : {{this.teamName}}</strong><br>
                {{#each this.teamMembers}}
                {{this.name}} ({{this.regNum}}) <br>
                {{/each}}
                {{/if}}
            </td>
            <td>{{this.classId}}</td>
            <td>
                {{#if (eq this.type "individual")}}
                {{this.participant.phone}}
                {{else}}
                {{this.contact.teamPhone}} / {{this.contact.altPhone}}
                {{/if}}
            </td>
            <td>{{this.timestampFormatted}}</td>
        </tr>
        {{/each}}
    </tbody>
</table>
{{else}}
<p class="text-muted">No registrations yet.</p>
{{/if}}